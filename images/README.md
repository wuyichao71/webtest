# images/

把你想用的**立绘图片**放在这里（建议 PNG 透明背景，尺寸 600×900 左右）。

然后在 `script.js` 的 `CHARS` 数组里，给某个角色加上一行 `image` 字段即可自动切换成图片立绘：

```js
{
  id: "hikari",
  name: "ひかり",
  title: "本作の主人公",
  image: "images/hikari.png",   // ← 加这一行，就会用图片代替 SVG 立绘
  ...
}
```

> 请使用你有权使用的图片。官方游戏素材受版权保护，不适合直接放在公开网站上。
